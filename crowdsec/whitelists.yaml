name: getarp/whitelists
description: "Trusted admin IPs — never alert/ban on traffic from these, regardless of what Suricata/cowrie see"
whitelist:
  reason: "trusted admin IP (management SSH)"
  ip:
    - "83.175.184.74"
